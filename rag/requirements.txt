# --- Transcription (Whisper) ---
# NOTE: openai-whisper requires ffmpeg to be installed and on PATH.
openai-whisper>=20231117

# --- RAG / vector store ---
chromadb>=0.5.0
sentence-transformers>=3.0.0

# --- LLM ---
groq>=0.13.0

# --- Auth (validate backend-issued JWTs to scope data per user) ---
python-jose[cryptography]>=3.3.0

# --- Database (shared PostgreSQL backend) ---
sqlalchemy>=2.0.36
psycopg[binary]>=3.2.0

# --- Config / utils ---
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.1

# --- Optional API layer ---
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.20
